import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

const logo: React.FC = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default logo;
